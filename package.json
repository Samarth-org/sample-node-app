{
  "name": "sample-node-app",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Building...' && mkdir -p dist && cp -r src/* dist/",
    "test":  "jest --coverage",
    "lint":  "eslint src/"
  },
  "devDependencies": {
    "jest":   "^29.0.0",
    "eslint": "^8.0.0"
  }
}